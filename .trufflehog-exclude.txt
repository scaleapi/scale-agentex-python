# Package manager lock files (contain integrity hashes that trigger false positives)
yarn\.lock
package-lock\.json
pnpm-lock\.yaml
Pipfile\.lock
uv\.lock

# Other common false positive sources
go\.sum
Cargo\.lock
Gemfile\.lock
poetry\.lock
composer\.lock
