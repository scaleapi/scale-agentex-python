# Agentex SDK
agentex-sdk

# Scale GenAI Platform Python SDK
scale-gp

# Temporal with the LangGraph plugin (temporalio.contrib.langgraph).
# The plugin runs LangGraph nodes as Temporal activities; needs >=1.27.0.
temporalio[langgraph]>=1.27.0

# LangChain model + tools
langchain-openai
langchain-core

# Optional: enables get_graph_ascii() ASCII graph rendering
grandalf

python-dotenv
